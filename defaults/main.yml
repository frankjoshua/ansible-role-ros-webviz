---
ros_ip: "{{ ansible_default_ipv4.address | default('127.0.0.1') }}"
ros_master_uri: 'http://{{ ros_ip }}:11311'
docker_name: 'ros_webviz'
pull_image: false
docker_image: 'frankjoshua/ros-webviz:latest'
port: '8080'
